/**
 * Created by schandramouli on 12/16/16.
 */
public class SingleResponsibility {
    // any class should only have one reason to change, i.e. one single responsibility
    // it should not have any other reason to change, e.g.  a reports class can be
    // responsible for maintaining reports (storing/retrieving), however, printing or
    // exporting the reports should be a separate class.
}
